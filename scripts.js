// Soma de números e impressão
function somarNumeros(a, b) {
  const resultado = a + b;
  console.log(`${a} + ${b} = ${resultado}`);
  return resultado;
}

// Exemplo de uso
somarNumeros(5, 3);
somarNumeros(10, 20);
somarNumeros(100, 50);
